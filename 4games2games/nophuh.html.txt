<!DOCTYPE html>
<html lang="el">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>4Games2Games</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: #121212;
      color: #eee;
    }
    header {
      background: #1e1e1e;
      color: #fff;
      padding: 1rem;
      text-align: center;
      border-bottom: 2px solid #333;
    }
    header h1 { margin:0; font-size:2rem; }
    header p { margin:5px 0 0; color:#aaa; }

    .container { max-width:1100px; margin:20px auto; padding:0 20px; }

    .categories { text-align:center; margin-bottom:20px; }
    .categories button {
      margin:5px;
      padding:8px 16px;
      border:none;
      border-radius:20px;
      cursor:pointer;
      background:#333;
      color:#fff;
      transition:background 0.3s;
    }
    .categories button:hover { background:#555; }

    .articles {
      display:grid;
      grid-template-columns:repeat(auto-fit,minmax(280px,1fr));
      gap:20px;
    }

    .card {
      background:#1e1e1e;
      border-radius:12px;
      overflow:hidden;
      box-shadow:0 4px 10px rgba(0,0,0,0.4);
      cursor:pointer;
      transition:transform 0.3s, box-shadow 0.3s;
    }
    .card:hover { transform:translateY(-5px); box-shadow:0 6px 15px rgba(0,0,0,0.6); }
    .card img { width:100%; height:160px; object-fit:cover; }

    .card-content { padding:15px; }
    .card-content h2 { margin:0 0 10px; font-size:1.2rem; }
    .card-content p { margin:0; color:#bbb; font-size:0.95rem; }
    .category-label {
      display:inline-block;
      background:#333;
      padding:3px 8px;
      border-radius:8px;
      font-size:0.8rem;
      margin-bottom:8px;
      color:#ccc;
    }

    form { margin-top:40px; background:#1e1e1e; padding:20px; border-radius:12px; box-shadow:0 2px 6px rgba(0,0,0,0.4); }
    input, textarea, select {
      width:100%; padding:10px; margin:8px 0; border:none; border-radius:8px; background:#2a2a2a; color:#fff;
    }
    button.submit {
      width:100%; padding:12px; background:#6200ee; color:#fff; border:none; border-radius:8px; cursor:pointer; font-size:1rem; transition:background 0.3s;
    }
    button.submit:hover { background:#7a2bff; }

    /* Modal */
    .modal { display:none; position:fixed; z-index:1000; left:0; top:0; width:100%; height:100%; overflow:auto; background: rgba(0,0,0,0.8); }
    .modal-content { background:#1e1e1e; margin:10% auto; padding:20px; border-radius:12px; width:90%; max-width:700px; position:relative; color:#eee; }
    .close { position:absolute; top:10px; right:20px; font-size:28px; font-weight:bold; cursor:pointer; color:#fff; }
    .modal img { width:100%; max-height:300px; object-fit:cover; margin-bottom:15px; border-radius:8px; }
  </style>
</head>
<body>
  <header>
    <h1>4Games2Games</h1>
    <p>Άρθρα για όλα τα παιχνίδια!</p>
  </header>

  <div class="container">
    <div class="categories">
      <button onclick="filterArticles('Όλα')">Όλα</button>
      <button onclick="filterArticles('Roblox')">Roblox</button>
      <button onclick="filterArticles('PC')">PC</button>
      <button onclick="filterArticles('Mobile')">Mobile</button>
      <button onclick="filterArticles('Retro')">Retro</button>
    </div>

    <div id="articles" class="articles"></div>

    <h2>➕ Νέο Άρθρο</h2>
    <form id="articleForm">
      <input type="text" id="title" placeholder="Τίτλος" required>
      <input type="text" id="image" placeholder="URL εικόνας (προαιρετικό)">
      <textarea id="content" placeholder="Περιγραφή άρθρου" required></textarea>
      <select id="category" required>
        <option value="Roblox">Roblox</option>
        <option value="PC">PC</option>
        <option value="Mobile">Mobile</option>
        <option value="Retro">Retro</option>
      </select>
      <button type="submit" class="submit">Προσθήκη</button>
    </form>
  </div>

  <!-- Modal -->
  <div id="modal" class="modal">
    <div class="modal-content">
      <span class="close" onclick="closeModal()">&times;</span>
      <img id="modalImage" src="" alt="Άρθρο">
      <h2 id="modalTitle"></h2>
      <p id="modalContent"></p>
    </div>
  </div>

  <script>
    const articles = [
      { title:"Steal a Brainrot", image:"https://via.placeholder.com/600x300", content:"Ένα διασκεδαστικό meme game που έγινε viral στο Roblox.", category:"Roblox" },
      { title:"Geometry Dash", image:"https://via.placeholder.com/600x300", content:"Το κλασικό rhythm-based παιχνίδι με χιλιάδες πίστες.", category:"Mobile" }
    ];

    let currentFilter = 'Όλα';

    function renderArticles() {
      const container = document.getElementById('articles');
      container.innerHTML = '';
      articles.filter(a => currentFilter==='Όλα'||a.category===currentFilter).forEach((article,i)=>{
        const div = document.createElement('div');
        div.className='card';
        div.innerHTML=`
          ${article.image?`<img src='${article.image}' alt='${article.title}'>`:''}
          <div class='card-content'>
            <span class='category-label'>${article.category}</span>
            <h2>${article.title}</h2>
            <p>${article.content.substring(0,80)}...</p>
          </div>
        `;
        div.onclick=()=>openModal(article);
        container.appendChild(div);
      });
    }

    function filterArticles(category){ currentFilter=category; renderArticles(); }

    document.getElementById('articleForm').addEventListener('submit',function(e){
      e.preventDefault();
      const title=document.getElementById('title').value;
      const image=document.getElementById('image').value;
      const content=document.getElementById('content').value;
      const category=document.getElementById('category').value;
      articles.push({title,image,content,category});
      renderArticles();
      this.reset();
    });

    function openModal(article){
      document.getElementById('modal').style.display='block';
      document.getElementById('modalImage').src=article.image||'';
      document.getElementById('modalTitle').innerText=article.title;
      document.getElementById('modalContent').innerText=article.content;
    }

    function closeModal(){ document.getElementById('modal').style.display='none'; }

    window.onclick=function(e){ if(e.target==document.getElementById('modal')) closeModal(); }

    renderArticles();
  </script>
</body>
</html>

